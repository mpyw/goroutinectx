{
  "title": "iter.MapErr callback context usage",
  "targets": [
    "conc"
  ],
  "level": "iter",
  "variants": {
    "good": {
      "description": "iter.MapErr callback uses context.",
      "functions": {
        "conc": "goodIterMapErr"
      }
    },
    "bad": {
      "description": "iter.MapErr callback does not use context.",
      "functions": {
        "conc": "badIterMapErr"
      }
    }
  }
}
